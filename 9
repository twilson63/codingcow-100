Now to access code from that included 
module, the included module is required
to have an exports interface.

module.exports = function() { 
  console.log('Hello World'); };

This is the most simplest export of a module
as a single function.

Cow Says: ./n
