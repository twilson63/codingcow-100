NodeJS uses a implemented version of CommonJS
for its module implementation:

To require a javascript module you simply:

require([filepath and name])
or
require([name])

The require([name]) function looks for modules
installed in the `node_modules` directory.

Cow Says: ./n
