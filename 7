The `package.json` can have special script commands

{ 
  ...
  "scripts": {
    "start": "beefy app.js 3000 --live"
  }
}

This command starts the beefy server on port 3000
and automatically generates an index.html page.

Cow Says: ./n
